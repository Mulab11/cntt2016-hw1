#include<bits/stdc++.h>
using namespace std;
int i,j,a[128],k,x,n,S;
class NewArenaPassword
{
	public:
		int minChange(string s,int K)
		{
			for(n=s.size();i<n-K;S+=x-k,i++)for(memset(a,x=k=0,sizeof(a)),j=i;j<n;j+=n-K)x++,k=max(k,++a[s[j]]);
			return S;
		}
};
